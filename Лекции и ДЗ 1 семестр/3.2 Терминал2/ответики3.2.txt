1. Какого типа команда cd? Попробуйте объяснить, почему она именно такого типа; опишите ход своих мыслей, если считаете что она могла бы быть другого типа.

type cd сообщает, что команда является встроенной в оболочку. я не очень понял суть вопроса, сломал всю голову и не мог начать ДЗ выполнять, потому что не могу дать ответ на этот вопрос:) поэтому попробую пролить немного бреда, ответив как понял.если верно считаю, то cd - это команда перенаправления. она переводит ваш tty в указанную директорию, перевод "каретки" вашего tty  в папку или иными словами производит "чтение" указанной директории,т.к. в Линукс все - файл. непросто было изложить структурированно ту кашу, что содержится у меня в голове по этому вопросу, но больше размышлений у меня на этот счет не возникло, к сожалению.

2. Какая альтернатива без pipe команде grep <some_string> <some_file> | wc -l? man grep поможет в ответе на этот вопрос. Ознакомьтесь с документом о других подобных некорректных вариантах использования pipe.
grep -c <some_string> <some_file>. такой вариант я сделал, почитав man grep и поэкспериментировав с файлом самостоятельно. c- показывает количесвто совпадений. 

3. Какой процесс с PID 1 является родителем для всех процессов в вашей виртуальной машине Ubuntu 20.04?
cat /proc/1/status | grep Name ответит нам, что это процесс systemd. ну, либо просто просмотреть cat /proc/1/status

4. Как будет выглядеть команда, которая перенаправит вывод stderr ls на другую сессию терминала?
ls 2>/dev/ttyN, гдн N - номер консоли. правда при подключении по ssh к ВМ консоль пишет, что нет доступа при попытке вызвать эту команду.

5. Получится ли одновременно передать команде файл на stdin и вывести ее stdout в другой файл? Приведите работающий пример.
Пример, в котором это так и работает cat fileIN > fileOUT

6. Получится ли находясь в графическом режиме, вывести данные из PTY в какой-либо из эмуляторов TTY? Сможете ли вы наблюдать выводимые данные?
Вывести получится, достаточно перенаправить вывод в целевую TTY, предварительно в ней авторизовавшись, переключившись по ctrl+alt+F3, например:
echo 123 > /dev/tty3  если не авторизоваться, получим сообщение Permission denied
Чтобы наблюдать вывод, необходимо переключиться по ctrl+alt+F3. Одновременно инициализировать вывод данных в TTY и наблюдать вывод не удастся. или я не знаю как:)

7. Выполните команду bash 5>&1. К чему она приведет? 
мы создаем файловый деcкриптор 5 и включаем вывод из него в stdout консоли.
Что будет, если вы выполните echo netology > /proc/$$/fd/5? Почему так происходит?
произойдет вывод   netology, т.к. предыдуйщей командой мы выполнили перенаправление файлового потока на stdout консоли.

8. Получится ли в качестве входного потока для pipe использовать только stderr команды, не потеряв при этом отображение stdout на pty? Напоминаем: по умолчанию через pipe передается только stdout команды слева от | на stdin команды справа. Это можно сделать, поменяв стандартные потоки местами через промежуточный новый дескриптор, который вы научились создавать в предыдущем вопросе.
получится. необходимо выполнить конструкцию команд и перенаправить , например декриптор 5 в stdout и после в него перенаправить stderror команды, результат выполнения которой пошлет ошибку.

echo 12333 > filein && cat filein && ls -z 5>&1 2>&5 | wc -l

Результат:
12333
2
вывод 12333 и количества строк в stderror ls -z т.к. параметра -z нет в ls

9. Что выведет команда cat /proc/$$/environ? Как еще можно получить аналогичный по содержанию вывод?


зпустили ping ya.ru
свернули ctrl+z
узнали PID фонового процесса jobs -l (1934)
отказались от дазаний текущего родителя disown ping 
 подключились к фоновому процессу reptyr 1934